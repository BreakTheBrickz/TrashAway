<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/home_icon"
            android:title="Home" />
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/settings_icon"
            android:title="Settings" />
        <item
            android:id="@+id/nav_trash_abc"
            android:icon="@drawable/trash_abc_icon"
            android:title="Trash ABC" />
    </group>

    <group android:checkableBehavior="single">
        <item android:title="Profile">
            <menu>
                <item
                    android:id="@+id/nav_login"
                    android:icon="@drawable/login_icon"
                    android:title="Login" />
                <item
                    android:id="@+id/nav_logout"
                    android:icon="@drawable/logout_icon"
                    android:title="Logout" />
                <item
                    android:id="@+id/nav_profile"
                    android:icon="@drawable/profile_icon"
                    android:title="Profile" />
            </menu>
        </item>
    </group>

    <item android:title="Trash">
        <menu>
            <item
                android:id="@+id/nav_throw_away_icon"
                android:icon="@drawable/throw_away_icon"
                android:title="Throw Away" />
            <item
                android:id="@+id/nav_add_bin_icon"
                android:icon="@drawable/add_bin_icon"
                android:title="Add Bin" />
        </menu>
    </item>

</menu>