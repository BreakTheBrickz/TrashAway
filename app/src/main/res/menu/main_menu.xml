<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Group creation for clarity and easier operation -->
    <group android:checkableBehavior="single">
        <item android:id="@+id/home"
            android:icon="@drawable/home_icon"
            android:title="Home" />
        <item android:id="@+id/settings"
            android:icon="@drawable/settings_icon"
            android:title="Settings" />
        <item android:id="@+id/trash_abc"
            android:icon="@drawable/trash_abc_icon"
            android:title="Trash ABC" />
    </group>

    <item android:title="Profile">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/login"
                    android:icon="@drawable/login_icon"
                    android:title="Login" />
                <item
                    android:id="@+id/logout"
                    android:icon="@drawable/logout_icon"
                    android:title="Logout" />
                <item
                    android:id="@+id/profile"
                    android:icon="@drawable/profile_icon"
                    android:title="Profile" />
            </group>
        </menu>
    </item>

    <item android:title="Trash">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/throw_away"
                    android:icon="@drawable/throw_away_icon"
                    android:title="Throw Away" />
                <item
                    android:id="@+id/add_bin"
                    android:icon="@drawable/add_bin_icon"
                    android:title="Add Bin" />
            </group>
        </menu>
    </item>

</menu>